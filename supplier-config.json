{
  "store": "pets",
  "cjDropshipping": {
    "enabled": true,
    "query": "pet supplies",
    "catalogMode": "live-curated-winners",
    "storefrontCap": 150,
    "note": "Live CJ API curated to top ~120–150 winners (listedNum/sales/price/EU cues). totalRecords may stay high in meta; products[] is capped for boutique storefront."
  },
  "germanDrop": {
    "enabled": true,
    "mode": "shared-account-sourcing"
  },
  "markets": [
    "NO",
    "EU",
    "PE"
  ],
  "printify": {
    "enabled": true,
    "shopId": "28847802",
    "catalogMode": "live-sector-with-selected",
    "catalogFile": "catalog/printify-selected.json",
    "note": "Sector-filtered Printify catalog mirrors CJ q=; printify-selected.json is the per-store curated set (~12 items). Printify left as-is this ship. Printify left as-is this ship. Printify left as-is this ship. Printify left as-is this ship.",
    "sectorQuery": "pet supplies",
    "matchTypes": [
      "tote",
      "tee"
    ]
  },
  "gelato": {
    "enabled": true,
    "catalogMode": "live-api-primary",
    "catalogFile": "catalog/gelato-products.json",
    "note": "Live Gelato API via GELATO_API_KEY (store first, then public catalog). Local JSON is empty fallback only — never selected clones.",
    "sectorQuery": "pet supplies"
  },
  "printful": {
    "enabled": true,
    "catalogMode": "live-api-primary",
    "catalogFile": "catalog/printful-products.json",
    "note": "Live Printful API via PRINTFUL_API_TOKEN (store first, then public catalog). Local JSON is empty fallback only — never selected clones.",
    "sectorQuery": "pet supplies"
  }
}
